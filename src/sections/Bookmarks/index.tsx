import React from "react";
import { AppHeader, AppFooter } from "../../utils/components";

export const Bookmarks = () => {
    return (
        <>
            <AppHeader />
            bookmarks
            <AppFooter />
        </>
    );
};
