import React from "react";
import { AppHeader, AppFooter } from "../../utils/components";

export const Feeds = () => {
    return (
        <>
            <AppHeader />
            feeds
            <AppFooter />
        </>
    );
};
